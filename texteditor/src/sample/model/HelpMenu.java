package sample.model;

import sample.controller.Controller;

public class HelpMenu {
    Controller controller;
    Model model;
    public HelpMenu(Controller controller, Model model){
        this.controller = controller;
        this.model = model;
    }
}
